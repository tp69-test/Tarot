[build]
  publish = "public"
  functions = "netlify/functions"

# Cấu hình chuyển hướng để Frontend dễ dàng gọi Backend
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
